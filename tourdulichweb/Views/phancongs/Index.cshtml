@model IEnumerable<Core.phancong>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.doandulich.tendoan)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nhanvien.tennhanvien)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nhiemvu.tennhiemvu)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.doandulich.tendoan)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nhanvien.tennhanvien)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nhiemvu.tennhiemvu)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
